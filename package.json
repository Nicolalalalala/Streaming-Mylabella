{
  "name": "streaming-mylabella-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash -lc 'if [ -f ~/.config/cloudflare/wrangler.env ]; then set -a; source ~/.config/cloudflare/wrangler.env; set +a; fi; wrangler deploy \"$@\"' bash",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250404.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
